package carrierdispatch;

/**
 * The AMBIGUITY control: two hierarchy-typed components, so which slot holds the
 * successor is genuinely undecidable. Declining is the soundness-preserving
 * answer; picking one would publish a resolved edge to a state chosen by field
 * order, which is a fabrication dressed as a result.
 */
public record Pair(Signal first, Signal second) {
}
